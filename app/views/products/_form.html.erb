<%= form_for(@product, html: {multipart: true}) do |f| %>
  <% if @product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% @product.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<!--
  <div class="field">
    <%= f.label :codigo %><br />
    <%= f.text_field :codigo %>
  </div> -->
  <div class="field">
    <%= f.label "Nombre producto" %><br />
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.label "Categoría "%><br />
    <%= f.select(:categoria, options_for_select(['Bebidas','Frutas','Verduras','Preparados'])) %>
  </div>
  <div class="field">
    <%= f.label "Precio" %><br />
    <%= f.number_field :precio %>
  </div>
  <div class="field">
    <%=f.label "Imagen" %>
    <%=f.file_field :photo   %>
  </div>
  <li>Datos personales</li>
    <div class="field">
    <%=f.label :user_id %>
    <%= f.select(:user_id, options_for_select([current_user.id])) %>
  </div>
  <div class="field">
    <%=f.label :email %>
    <%= f.select(:email, options_for_select([current_user.email])) %>
  </div>    
  <div class="actions">
    <%= f.submit 'Crear producto', :class=>"btn btn-primary" %>
  </div>
<% end %>
